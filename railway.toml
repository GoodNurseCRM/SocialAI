[build]
builder = "nixpacks"

[deploy]
startCommand = "streamlit run saas_app.py --server.port $PORT --server.address 0.0.0.0 --server.headless true"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[healthcheck]
path = "/"
timeout = 30
